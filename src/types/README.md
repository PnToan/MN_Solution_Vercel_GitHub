# Types / Data Contracts

Khu vực này dùng để chuẩn hóa dạng dữ liệu khi dự án phát triển lớn hơn.

## Panel

```js
{
  id, name, zoneId, orientation, x, y, width, height, thickness, color
}
```

## Zone

```js
{
  id, name, x, y, width, height, bounds
}
```
